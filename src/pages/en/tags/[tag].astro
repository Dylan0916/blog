---
import TagPostsPage from "@/components/pages/TagPostsPage.astro";
import { getTagStaticPaths } from "@/utils/getTagStaticPaths";

export const getStaticPaths = getTagStaticPaths;

interface Props {
  tag: string;
}

const { tag } = Astro.props as Props;
---

<TagPostsPage tag={tag} />
